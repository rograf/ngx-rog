import { RogSortByPipe } from './rog-sort-by.pipe';

describe('RogSortByPipe', () => {
  it('create an instance', () => {
    const pipe = new RogSortByPipe();
    expect(pipe).toBeTruthy();
  });
});
